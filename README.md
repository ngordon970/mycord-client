# mycord-client
Multi-threaded command-line chat client written in C using POSIX sockets. Implements a custom binary protocol for login, messaging, and graceful shutdown, with support for username mentions and concurrent I/O.
